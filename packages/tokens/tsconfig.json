{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    /* Same reasoning as packages/core: consumed by Hermes as well as browsers. */
    "lib": ["esnext"],
    "types": ["node"],
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
