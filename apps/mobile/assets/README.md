# Placeholder assets

`icon.png`, `adaptive-icon.png` and `splash.png` here are solid `#fdf9ee`
rectangles (the light-theme `--background` token) at the sizes `app.config.ts`
expects, generated so the Expo config has real files to point at. They are not
final artwork — replace them in Task 23 (store readiness), which also needs
proper app icon variants for iOS/Android and a splash screen that does not
just match the background colour.
